apiVersion: apps/v1
kind: Deployment
metadata:
  name: nglab-api
spec:
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: "1000m"
              memory: "2Gi"
            limits:
              cpu: "4000m"
              memory: "8Gi"
