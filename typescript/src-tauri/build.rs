//! Build script for the NGLab Tauri application.
//!
//! Handles Tauri-specific build optimizations and resource bundling.

/** Entry point for the Tauri build process. */
fn main() {
    tauri_build::build()
}
